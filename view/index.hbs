<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        @import url('https://fonts.googleapis.com/css?family=Muli:400,400i,700,700i');

        a {
            text-decoration: none;
            color: black;
        }

        .shell .bigo {
            margin-top: 20px;
            display: flex;
            padding: 20px;
        }

        .shell .bigo .hdt h1 {
            font-size: 45px;
            padding-left: 73px;
            font-weight: 100;
            margin: 0;
        }

        .shell .bigo .hdt {
            width: 100%;
        }

        .shell .bigo .hdt .line {
            width: 8rem;
            height: .35rem;
            background-color: lightseagreen;
            border-radius: .25rem;
            margin-left: 73px;
            transition: all 0.5s ease;
        }

        .shell .bigo .hdt h1:hover+.line {
            width: 15rem;
        }

        .shell .bigo .prt p {
            font-size: 16px;
            float: right;
            padding-left: 150px;
            width: 100%;
            margin-right: 40px;
        }

        .row {
            display: flex;
            padding: 60px;
            overflow: auto;
            font-family: 'Muli', sans-serif;
        }

        .row::-webkit-scrollbar {
            width: 10px;
            height: 10px;
            /* margin-top: -50px; */
            background: #20b2aa29;
        }

        /* Track */
        .row::-webkit-scrollbar-track {
            border-radius: 10px;
        }

        /* Handle */
        .row::-webkit-scrollbar-thumb {
            background: lightseagreen;
            border-radius: 20px;
        }

        .wsk-cp-product {
            box-shadow: inset 0px 0px 20px 0px rgb(0 0 0 / 30%);
            padding: 10px;
            border-radius: 10px;
            position: relative;
            margin: 15px;
            cursor: pointer;
            background: white;
        }

        .wsk-cp-img {
            width: 100%;
            transition: all 0.1s ease-in-out;
        }

        .wsk-cp-img img {
            width: 310px;
            transition: all 0.1s ease-in-out;
            border-radius: 6px;
            height: 220px;
        }

        .wsk-cp-product:hover .wsk-cp-img {
            transform: translate(0, -35px);
        }

        .wsk-cp-product:hover .wsk-cp-img img {
            box-shadow: 0 19px 38px rgba(0, 0, 0, 0.30), 0 15px 12px rgba(0, 0, 0, 0.22);
        }

        .wsk-cp-text .category {
            text-align: center;
            font-size: 12px;
            font-weight: bold;
            padding: 5px;
            margin-top: -10px;
            position: relative;
            transition: all 0.2s ease-in-out;
        }

        .wsk-cp-text .category span {
            padding: 12px 30px;
            border: 1px solid #313131;
            background: #212121;
            color: #fff;
            box-shadow: 0 19px 38px rgba(0, 0, 0, 0.30), 0 15px 12px rgba(0, 0, 0, 0.22);
            border-radius: 27px;
            transition: all 0.05s ease-in-out;

        }

        .wsk-cp-product:hover .wsk-cp-text .category span {
            border-color: #ddd;
            box-shadow: none;
            padding: 11px 28px;
        }

        .wsk-cp-product:hover .wsk-cp-text .category span {
            background: lightseagreen;
        }

        .wsk-cp-text .title-product {
            text-align: center;
        }

        .wsk-cp-text .title-product h3 {
            font-size: 20px;
            font-weight: bold;
            margin: 15px auto;
            overflow: hidden;
            white-space: nowrap;
            text-overflow: ellipsis;
            width: 100%;
        }

        .wsk-cp-text .description-prod p {
            margin: 0;
            width: 310px;
        }

        /* Truncate */
        .wsk-cp-text .description-prod {
            text-align: center;
            width: 100%;
            height: 75px;
            overflow: hidden;
            display: -webkit-box;
            -webkit-line-clamp: 3;
            -webkit-box-orient: vertical;
            margin-bottom: 15px;
        }

        .arrow {
            display: grid;
            align-items: center;
        }

        @keyframes arrow {
            0% {
                opacity: 0;
            }

            100% {
                opacity: 1;
            }
        }

        .leftico,
        .righthico {
            position: absolute;
            margin: 10px;
            font-weight: 900;
            font-size: 30px;
            animation: arrow 1s linear infinite;
        }

        .leftico {
            left: 0;
        }

        .righthico {
            right: 0;
        }

        @media only screen and (max-width: 1000px) {
            .row {
                padding: 40px;
            }

            .wsk-cp-text .description-prod p {
                margin: 0;
                width: 300px;
            }

            .shell .bigo .prt p {
                width: 60%;
                padding-left: 10px;
            }

            .wsk-cp-img img {
                width: 300px;
                height: 240px;
            }
        }

        @media only screen and (max-width: 550px) {
            .shell .bigo {
                display: grid;
            }

            .row {
                padding: 10px;
            }

            .shell .bigo .hdt h1 {
                font-size: 35px;
                padding-left: 2px;
            }

            .shell .bigo .hdt .line {
                margin-left: 2px;
            }

            .shell .bigo .prt p {
                width: 100%;
                font-size: 15px;
                margin-right: 15px;
                margin-top: 10px;
                padding-left: 16px;
            }

            .wsk-cp-img img {
                width: 270px;
                height: 220px;
            }

            .wsk-cp-text .description-prod p {
                margin: 0;
                width: 270px;
            }

        }
    </style>
</head>

<body>
    {{>navbar}}
<div class="row">
    {{#each destinations}}
    <div class="col-md-3">
        <div class="wsk-cp-product"> <a href="tourpage?tour='.$places.'&name='.$codename.'">
                <div class="wsk-cp-img">
                    <img src="https://nxttour.in/{{hidecode}}" alt="Product" class="img-responsive" />
                </div>
            </a>
            <div class="wsk-cp-text">
                <a href="tourpage?tour='.$places.'&name='.$codename.'">
                    <div class="category">
                        <span>Rs. {{rate}}</span>
                    </div>
                </a>
                <div class="title-product">
                    <a href="tourpage?tour='.$places.'&name='.$codename.'">
                        <h3>{{place}}</h3>
                    </a>
                </div>
                <div class="description-prod">
                    <p>{{desc}}</p>
                </div>
            </div>
        </div>
    </div>
    {{/each}}
    </div>
    {{>footer}}
</body>

</html>